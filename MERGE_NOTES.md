# Merge Notes

בוצע איחוד וסידור של הפרויקט כדי שהאפליקציה תיבנה ותעבוד בצורה עקבית.

## שינויים מרכזיים

- תוקן ייבוא `Login` באמצעות התאמת שם הקובץ `Login.jsx`.
- הוסרה תיקיית `src/components/shoval` לאחר איחוד הרכיבים הרלוונטיים לתיקיית `components` הראשית.
- מסך ההרשמה הציבורי מחובר כעת לקומפוננטת `RegistrationScreen` במקום placeholder.
- `AdminDashboard` מחובר למסכי קבוצות, מתנדבים, מדריכים, אירועים ודוחות.
- `GuideDashboard` עובד ללא תלות ב־React Router ומציג תפריט פנימי: הקבוצה שלי, נוכחות ומתנדבים.
- ניהול קבוצות מסנכרן שיוך מדריך גם במסמך הקבוצה וגם במסמך המדריך.
- ניהול מתנדבים תומך בסינון לפי קבוצה ובשמירת `groupId` + `groupName`.
- מסך נוכחות תומך בקבוצה נעולה עבור מדריכים ושומר רשומות נוכחות אחידות.
- הוסר `react-router-dom` כי הוא כבר לא בשימוש.
- נוספו סקריפטים להרצה מהשורש וקובץ `frontend/.env.example`.
- הוסרו קבצים זמניים/בדיקות מהשורש.

## בדיקות

הפקודות הבאות עברו בהצלחה:

```bash
npm run build
npm run lint
```

ה־build מציג אזהרת גודל bundle של Vite בגלל Firebase והמסכים הרבים. זו אזהרה בלבד, לא שגיאת build.
