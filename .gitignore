# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backend/privateKey.json
.env*

# Firebase
.firebase/
# NOTE: .firebaserc IS committed on purpose — it only holds the default project
# id (shalva-jce), which is public (it's part of the hosting URL), and both local
# and CI `firebase deploy` need it to know which project to target.
.env.local
# Playwright E2E artifacts (screenshots, reports, traces)
test-results/
playwright-report/
